name = "image-search-hub"
main = "./worker.js"
compatibility_date = "2026-09-16"

[assets]
directory = "."

# IMAGE_KV is bound to this Worker in the Cloudflare dashboard.
# Keep the namespace ID out of the repository configuration.
